import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.lecibur.homecloud',
  appName: 'HomeCloud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
